pre-commit:
  parallel: true
  jobs:
    - name: cargo fmt
      glob: "*.rs"
      run: cargo fmt --all -- --check

    - name: cargo clippy
      glob: "*.rs"
      run: cargo clippy --all-targets --all-features -- -D warnings
